# Placeholder `deploy_build_before` hook for building theme assets locally
# and then copying the files to the remote server
#
# Uncomment the lines below and replace `sage` with your theme folder

---
- name: Update Slack
  uri:
    url: https://hooks.slack.com/services/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
    method: POST
    body: '{{ {"text": "This is posted to #digital and comes from *deploybot*.", "channel": "#digital", "link_names": 1, "username": "deploybot", "icon_emoji": ":robot_face:"} }}'
    body_format: json
    HEADER_Content-Type: "application/json"

